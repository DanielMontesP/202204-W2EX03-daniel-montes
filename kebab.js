const isKebabCase = (textToCheck) => {
  const x = "";
};

const formatKebabToLowerCamelCase = (textToCheck) => {};

const showMessageToUser = () => {};

isKebabCase(textToCheck)
  ? formatKebabToLowerCamelCase(textToCheck)
  : showMessageToUser();

isKebabCase("ohMama");
